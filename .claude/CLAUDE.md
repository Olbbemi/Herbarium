# knowledge-promote 커밋 워크플로

promote 결과를 저장할 때는 아래 순서를 반드시 지킨다.

1. `promote/<id>` 브랜치를 새로 만든다.
2. 확정지식 파일을 해당 브랜치에 쓴다 (커밋하지 않음).
3. 파일 경로를 사용자에게 알리고 검토를 요청한다. 파일 내용을 대화창에 통째로 출력하지 않는다.
4. 사용자가 승인하면 커밋 후 push 한다.
5. `gh pr create` 로 PR을 생성한다.
6. 이후 머지는 사용자가 직접 한다.

사용자 승인 전에 커밋하지 않는다.
